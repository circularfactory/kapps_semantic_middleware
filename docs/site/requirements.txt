# The docs toolchain, kept apart from the project's dev group so the docs build
# in GitLab CI installs only what it needs.
#
# Pinned to majors, not exact versions: each published version keeps its own
# build, so an old version is never rebuilt and this file only ever has to
# build the CURRENT version. That is what lets it float.
sphinx>=9,<10
pydata-sphinx-theme>=0.20,<1
myst-nb>=1.4,<2
sphinx-design>=0.6,<1
