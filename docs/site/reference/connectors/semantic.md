# Semantic registry

The registry that maps a capability onto the connector able to carry it.

```{eval-rst}
.. automodule:: kapps_semantic_middleware.connectors.semantic
   :members:
   :show-inheritance:
```
